description: '`FORBIDDEN` El servidor ha denegado la petición.'
content:
  application/problem+json:
    schema:
      $ref: '../schemas/HTTP_Problem.yaml'
    examples:
      response-http-403:
        $ref: '../examples/response-http-403.yaml'
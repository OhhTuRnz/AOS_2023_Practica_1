description: >-
  `UNPROCESSABLE ENTITY` Falta alguno de los atributos obligatorios o son
  incorrectos.
content:
  application/problem+json:
    schema:
      $ref: ../schemas/HTTP_Problem.yaml
    examples:
      response-http-422:
        $ref: ../examples/response-http-422.yaml
